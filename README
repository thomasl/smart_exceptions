
'stable' is the version of smart_exceptions previously on jungerl.

'devel' is the new version under development; it uses try instead of catch
to handle more exceptions correctly and is a simplification and rewrite of
the stable version. Status: needs much more testing.

